# AI 工作台 Cloudflare 免费部署完整指南

> 本文档记录从零搭建一个每日自动更新的 AI 资讯工作台的完整流程，包括 GitHub 仓库创建、Cloudflare Pages 部署、自动化推送等。
>
> **适用场景**：将本地静态网页发布为可手机访问的免费网站
> **技术栈**：macOS + Git + GitHub + Cloudflare Pages
> **最后更新**：2026-09-22

---

## 一、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│  本地机器 (Mac)                                              │
│                                                             │
│  ~/AI-News-Agent/                                           │
│    ├── main.py          ← 每日 8:00 cron 自动抓取新闻        │
│    ├── sources/         ← 数据源配置 (量子位/OpenAI/钛媒体)   │
│    └── output/          ← 生成 HTML + JSON 数据文件          │
│                                                             │
│  ~/Desktop/AI工作台/                                        │
│    ├── workbench.html     ← 嵌入数据的单文件网页             │
│    ├── ai-data-2026-09-22.json                             │
│    └── AI日报-2026-09-22.html                              │
│                                                             │
│  ~/pi/workbench-deploy/  ← Git 仓库 (推送到 GitHub)         │
│    ├── workbench.html                                    │
│    └── README.md                                          │
└─────────────────────────────────────────────────────────────┘
         │ git push (SSH)
         ▼
┌─────────────────────────────────────────────────────────────┐
│  GitHub (yancx8702/ai-workbench)                            │
│  存储代码，作为 Cloudflare 的数据源                           │
└─────────────────────────────────────────────────────────────┘
         │ 自动拉取
         ▼
┌─────────────────────────────────────────────────────────────┐
│  Cloudflare Pages                                           │
│  免费托管静态网站，自动生成 https://xxx.pages.dev            │
│  支持自定义域名绑定                                          │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
    手机 / 电脑浏览器访问
```

---

## 二、前置准备

### 2.1 需要的账号

| 平台 | 用途 | 是否需要付费 |
|------|------|-------------|
| GitHub | 代码托管 | 免费 |
| Cloudflare | 网站托管 | 免费 |

### 2.2 需要的工具

```bash
# 检查是否已安装
git --version      # 应输出类似 git version 2.39.x
python3 --version  # 应输出 Python 3.x.x
```

如果未安装 Git，在终端运行：
```bash
xcode-select --install
```

---

## 三、第一步：GitHub 仓库设置

### 3.1 创建仓库

1. 打开浏览器，访问：
   ```
   https://github.com/new
   ```

2. 填写创建表单：

   | 字段 | 填写内容 | 说明 |
   |------|----------|------|
   | Repository name | `ai-workbench` | 仓库名称（小写，用 `-` 连接） |
   | Owner | 你的 GitHub 用户名 | 选择你的个人账号或组织 |
   | Visibility | **Public**（公开） | 必须公开才能被 Cloudflare 读取 |
   | Add README | ❌ 不勾选 | 我们手动添加内容 |

3. 点击绿色按钮 **Create repository**

4. 创建完成后，记录页面上显示的仓库地址，格式为：
   ```
   git@github.com:你的用户名/ai-workbench.git
   ```

---

### 3.2 生成并配置 SSH 密钥

SSH 密钥用于让本地电脑免密码连接到 GitHub。

#### 3.2.1 检查是否已有密钥

打开终端，运行：
```bash
ls ~/.ssh/id_*.pub
```

- 如果输出 `~/.ssh/id_ed25519.pub` 或 `~/.ssh/id_rsa.pub`，说明已有密钥，跳到 **3.2.3**
- 如果输出 `ls: ... No such file or directory`，继续下一步

#### 3.2.2 生成新密钥

终端运行：
```bash
ssh-keygen -t ed25519 -C "workbench" -f ~/.ssh/id_ed25519 -N ""
```

参数说明：
- `-t ed25519`：使用 ED25519 算法（更安全，比 RSA 更快）
- `-C "workbench"`：密钥注释，方便识别
- `-f ~/.ssh/id_ed25519`：保存到默认位置
- `-N ""`：不设置密码（空字符串），实现完全免交互

终端会输出：
```
Your identification has been saved in /Users/你的用户名/.ssh/id_ed25519
Your public key has been saved in /Users/你的用户名/.ssh/id_ed25519.pub
```

#### 3.2.3 复制公钥

终端运行：
```bash
cat ~/.ssh/id_ed25519.pub
```

终端会输出一整行，格式如下：
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIxxxxxxxxxxxxxxxxxxxxxxx 你的邮箱
```

**全选复制这一整行**（从 `ssh-ed25519` 开始到邮箱结尾）

#### 3.2.4 添加到 GitHub

1. 打开：
   ```
   https://github.com/settings/keys
   ```

2. 点击右上角绿色按钮 **New SSH key**

3. 填写：
   - **Title**：`workbench`（或其他方便识别的名称）
   - **Key**：粘贴刚才复制的完整内容

4. 点击 **Add SSH key**

#### 3.2.5 验证连接

终端运行：
```bash
ssh -T git@github.com
```

如果看到以下提示，说明配置成功：
```
Hi 你的用户名! You've successfully authenticated, but GitHub does not provide shell access.
```

如果出现 `Permission denied`，请检查：
1. SSH key 是否正确添加到 GitHub
2. 终端运行 `ssh-add ~/.ssh/id_ed25519` 将密钥加入代理

---

## 四、第二步：准备部署文件

### 4.1 确定要部署的内容

本案例中需要部署的文件在：
```
~/Desktop/AI工作台/
├── workbench.html        ← 主网页（内嵌数据，单文件即可运行）
├── ai-data-2026-09-22.json ← 原始数据（可选，用于备份）
└── AI日报-2026-09-22.html  ← 详细日报（可选）
```

### 4.2 创建本地仓库目录

终端运行：
```bash
# 创建部署目录
mkdir -p ~/pi/workbench-deploy
cd ~/pi/workbench-deploy

# 复制文件
cp ~/Desktop/AI工作台/workbench.html .
cp ~/Desktop/AI工作台/ai-data-*.json .
cp ~/Desktop/AI工作台/AI日报-*.html . 2>/dev/null || true
```

### 4.3 初始化 Git 仓库

```bash
cd ~/pi/workbench-deploy
git init
git config user.email "你的邮箱@example.com"
git config user.name "你的名字"
git add .
git commit -m "Initial commit: AI 工作台首页"
```

---

## 五、第三步：推送到 GitHub

### 5.1 添加远程仓库

终端运行（将 `<你的用户名>` 替换为你的 GitHub 用户名）：
```bash
git remote add origin git@github.com:<你的用户名>/ai-workbench.git
```

验证是否添加成功：
```bash
git remote -v
```
应输出：
```
origin  git@github.com:<你的用户名>/ai-workbench.git (fetch)
origin  git@github.com:<你的用户名>/ai-workbench.git (push)
```

### 5.2 推送代码

```bash
git push -u origin main
```

首次推送可能需要输入 passphrase（如果生成密钥时设置了密码），否则直接推送成功。

成功后终端显示：
```
Enumerating objects: 5, done.
Counting objects: 100% (5/5), done.
Delta compression using up to 12 threads
Compressing objects: 100% (4/4), done.
Writing objects: 100% (5/5), 10.50 KiB | 5.25 MiB/s, done.
Total 5 (delta 0), reused 0 (delta 0), pack-reused 0
To github.com:<你的用户名>/ai-workbench.git
 * [new branch]      main -> main
branch 'main' set up to track 'origin/main'.
```

### 5.3 验证

打开浏览器访问：
```
https://github.com/<你的用户名>/ai-workbench
```
确认所有文件已上传。

---

## 六、第四步：Cloudflare Pages 部署

### 6.1 登录 Cloudflare

1. 打开：
   ```
   https://dash.cloudflare.com/sign-up
   ```
2. 注册账号（用邮箱或 Google 账号登录均可）
3. 登录后进入仪表盘：
   ```
   https://dash.cloudflare.com
   ```

### 6.2 创建 Pages 项目

1. 在左侧菜单找到 **Pages**，点击进入
2. 点击蓝色按钮 **Create project**
3. 在弹出窗口中选择 **Connect to Git**
4. 选择 **GitHub**，点击 **Connect** 授权
5. 在仓库列表中找到并点击 **ai-workbench**
6. 点击 **Begin setup**

### 6.3 配置构建设置

在配置页面，按以下要求填写：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| **Project name** | `ai-workbench` | 自动从仓库名获取，可修改 |
| **Production branch** | `main` | 主分支名 |
| **Framework preset** | **None** | 因为我们是纯静态文件，不需要构建框架 |
| **Build command** | *(留空)* | 纯静态网站不需要构建命令 |
| **Build output directory** | **`.`** | **必填一个点**，表示使用仓库根目录 |
| **Environment variables** | *(留空)* | 无需环境变量 |

> ⚠️ **常见错误**：`Build output directory` 填错会导致部署后页面空白。必须填 `.` （单独一个英文句点）

### 6.4 部署

1. 确认所有配置无误
2. 点击底部蓝色按钮 **Save and Deploy**
3. Cloudflare 会自动：
   - 从 GitHub 拉取最新代码
   - 编译构建（本案例无需构建）
   - 部署到全球 CDN 节点
4. 等待约 1-2 分钟，状态变为 **Deployed**

### 6.5 访问网站

部署成功后，你会看到：

1. **访问地址**：
   ```
   https://ai-workbench-xxx.pages.dev
   ```
   （`xxx` 是随机字符串，每个人的不同）

2. 点击 **Visit site** 即可打开网站

3. **手机访问**：
   - 用手机浏览器打开同一链接
   - 同样可以正常显示（响应式设计）

---

## 七、日常维护与更新

### 7.1 自动更新流程

如果配置了自动化脚本（如 cron + Git push），每天早晨会自动：
1. 抓取最新 AI 资讯
2. 生成新的 workbench.html
3. 推送到 GitHub
4. Cloudflare Pages 自动重新部署

### 7.2 手动更新步骤

如需手动推送新内容：

```bash
# 1. 进入部署目录
cd ~/pi/workbench-deploy

# 2. 替换最新文件
cp ~/Desktop/AI工作台/workbench.html .
cp ~/Desktop/AI工作台/ai-data-*.json .

# 3. 提交并推送
git add .
git commit -m "更新: $(date +%Y-%m-%d)"
git push
```

推送后约 30 秒内，Cloudflare Pages 会自动重新部署。

### 7.3 查看部署日志

1. 打开：
   ```
   https://dash.cloudflare.com/pages
   ```
2. 点击 **ai-workbench** 项目
3. 点击 **Deployments** 标签
4. 查看最新的部署记录和日志

---

## 八、自定义域名（可选）

如果你有已购买的域名，可以绑定到 Pages 站点。

### 8.1 在 Cloudflare 添加域名

1. 在 Cloudflare 仪表盘，点击 **Add a domain**
2. 输入你的域名（如 `news.yourdomain.com`）
3. 按照提示将 DNS 记录添加到你的 DNS 服务商

### 8.2 绑定到 Pages

1. 进入 Pages 项目设置
2. 点击 **Custom domains**
3. 点击 **Set up a custom domain**
4. 选择已添加的域名
5. Cloudflare 会自动配置 CNAME 记录

绑定成功后，可以通过 `https://news.yourdomain.com` 访问。

---

## 九、常见问题排查

### Q1：手机端打开显示空白

**原因**：浏览器缓存了旧版页面
**解决**：
- iOS Safari：长按刷新按钮 → 清空网站数据
- Android Chrome：设置 → 隐私 → 清除浏览数据
- 或使用无痕模式访问

### Q2：Cloudflare 显示 "Build failed"

**原因**：构建配置错误
**解决**：
1. 检查 `Build output directory` 是否为 `.`
2. 检查 `Framework preset` 是否选择了 **None**
3. 查看 Deployments 日志中的具体错误信息

### Q3：GitHub 推送被拒绝

**原因**：SSH 权限问题或分支冲突
**解决**：
```bash
# 检查 SSH 连接
ssh -T git@github.com

# 重新添加密钥到代理
ssh-add ~/.ssh/id_ed25519

# 强制推送（谨慎使用，会覆盖远端）
git push --force
```

### Q4：修改了文件但网站没更新

**原因**：Git 没有推送或 Cloudflare 缓存
**解决**：
```bash
# 确认文件已修改
git status

# 重新提交推送
git add .
git commit -m "fix: 更新内容"
git push

# 等待 1-2 分钟后强制刷新页面
```

---

## 十、费用说明

| 项目 | 费用 | 说明 |
|------|------|------|
| GitHub 仓库 | 免费 | 无限公开仓库 |
| Cloudflare Pages | 免费 | 每月 500 次构建，100GB 流量 |
| 自定义域名 | 域名本身费用 | 如阿里云购买约 50 元/年 |

**总计：完全免费**

---

## 附录：快速命令参考

```bash
# ========== 生成 SSH Key ==========
ssh-keygen -t ed25519 -C "workbench" -f ~/.ssh/id_ed25519 -N ""

# ========== 查看公钥（复制到 GitHub） ==========
cat ~/.ssh/id_ed25519.pub

# ========== 验证 SSH 连接 ==========
ssh -T git@github.com

# ========== 推送更新 ==========
cd ~/pi/workbench-deploy
git add .
git commit -m "更新: $(date +%Y-%m-%d)"
git push

# ========== 查看部署状态 ==========
curl -s https://api.github.com/repos/<用户名>/ai-workbench/deployments
```

---

*本文档基于实际部署经验编写，如有问题可随时调整。*
