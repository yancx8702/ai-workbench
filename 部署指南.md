# AI 工作台免费部署完整指南

> 本文档记录从零搭建一个每日自动更新的 AI 资讯工作台的完整流程。
>
> **适用场景**：将本地静态网页发布为可手机访问的免费网站
> **技术栈**：macOS + Git + GitHub Pages
> **最后更新**：2026-09-22

---

## 一、部署方案选择

### 1.1 方案对比

| 方案 | 优点 | 缺点 | 本案例选择 |
|------|------|------|-----------|
| **GitHub Pages** | 免费、无需额外账号、与代码同源 | 自定义域名配置稍复杂 | ✅ **最终采用** |
| Cloudflare Pages | 功能更强、CDN 更快 | 需要注册 Cloudflare 账号、国内访问可能受限 | ❌ 尝试过但因网络问题放弃 |

### 1.2 最终架构

```
┌─────────────────────────────────────────────────────────────┐
│  本地机器 (Mac)                                              │
│                                                             │
│  ~/AI-News-Agent/                                           │
│    ├── main.py          ← 每日 8:00 cron 自动抓取新闻        │
│    ├── sources/         ← 数据源配置 (量子位/OpenAI/钛媒体)   │
│    └── output/          ← 生成 HTML + JSON 数据文件          │
│                                                             │
│  ~/Desktop/AI工作台/                                        │
│    ├── workbench.html     ← 嵌入数据的单文件网页             │
│    ├── ai-data-2026-09-22.json                             │
│    └── AI日报-2026-09-22.html                              │
│                                                             │
│  ~/pi/workbench-deploy/  ← Git 仓库 (推送到 GitHub)         │
│    ├── workbench.html                                    │
│    ├── index.html           ← GitHub Pages 入口文件        │
│    └── .nojekyll          ← 禁用 Jekyll 构建              │
└─────────────────────────────────────────────────────────────┘
         │ git push (SSH)
         ▼
┌─────────────────────────────────────────────────────────────┐
│  GitHub (yancx8702/ai-workbench)                            │
│  ├── main 分支              ← 主代码分支                    │
│  └── gh-pages 分支          ← GitHub Pages 部署分支         │
└─────────────────────────────────────────────────────────────┘
         │ 自动部署
         ▼
┌─────────────────────────────────────────────────────────────┐
│  GitHub Pages                                               │
│  免费托管静态网站，自动生成 https://username.github.io/repo  │
│  支持自定义域名绑定                                          │
└─────────────────────────────────────────────────────────────┘
         │
         ▼
    手机 / 电脑浏览器访问
```

---

## 二、前置准备

### 2.1 需要的账号

| 平台 | 用途 | 是否需要付费 |
|------|------|-------------|
| GitHub | 代码托管 + 网站托管 | 免费 |

### 2.2 需要的工具

```bash
# 检查是否已安装
git --version      # 应输出类似 git version 2.39.x
python3 --version  # 应输出 Python 3.x.x
```

如果未安装 Git，在终端运行：
```bash
xcode-select --install
```

---

## 三、第一步：GitHub 仓库设置

### 3.1 创建仓库

1. 打开浏览器，访问：
   ```
   https://github.com/new
   ```

2. 填写创建表单：

   | 字段 | 填写内容 | 说明 |
   |------|----------|------|
   | Repository name | `ai-workbench` | 仓库名称（小写，用 `-` 连接） |
   | Owner | 你的 GitHub 用户名 | 选择你的个人账号 |
   | Visibility | **Public**（公开） | 必须公开才能使用 GitHub Pages |
   | Add README | ❌ 不勾选 | 我们手动添加内容 |

3. 点击绿色按钮 **Create repository**

4. 创建完成后，记录页面上显示的仓库地址，格式为：
   ```
   git@github.com:你的用户名/ai-workbench.git
   ```

---

### 3.2 生成并配置 SSH 密钥

SSH 密钥用于让本地电脑免密码连接到 GitHub。

#### 3.2.1 检查是否已有密钥

打开终端，运行：
```bash
ls ~/.ssh/id_*.pub
```

- 如果输出 `~/.ssh/id_ed25519.pub` 或 `~/.ssh/id_rsa.pub`，说明已有密钥，跳到 **3.2.3**
- 如果输出 `ls: ... No such file or directory`，继续下一步

#### 3.2.2 生成新密钥

终端运行：
```bash
ssh-keygen -t ed25519 -C "workbench" -f ~/.ssh/id_ed25519 -N ""
```

参数说明：
- `-t ed25519`：使用 ED25519 算法（更安全，比 RSA 更快）
- `-C "workbench"`：密钥注释，方便识别
- `-f ~/.ssh/id_ed25519`：保存到默认位置
- `-N ""`：不设置密码（空字符串），实现完全免交互

终端会输出：
```
Your identification has been saved in /Users/你的用户名/.ssh/id_ed25519
Your public key has been saved in /Users/你的用户名/.ssh/id_ed25519.pub
```

#### 3.2.3 复制公钥

终端运行：
```bash
cat ~/.ssh/id_ed25519.pub
```

终端会输出一整行，格式如下：
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIxxxxxxxxxxxxxxxxxxxxxxx 你的邮箱
```

**全选复制这一整行**（从 `ssh-ed25519` 开始到邮箱结尾）

#### 3.2.4 添加到 GitHub

1. 打开：
   ```
   https://github.com/settings/keys
   ```

2. 点击右上角绿色按钮 **New SSH key**

3. 填写：
   - **Title**：`workbench`（或其他方便识别的名称）
   - **Key**：粘贴刚才复制的完整内容

4. 点击 **Add SSH key**

#### 3.2.5 验证连接

终端运行：
```bash
ssh -T git@github.com
```

如果看到以下提示，说明配置成功：
```
Hi 你的用户名! You've successfully authenticated, but GitHub does not provide shell access.
```

如果出现 `Permission denied`，请检查：
1. SSH key 是否正确添加到 GitHub
2. 终端运行 `ssh-add ~/.ssh/id_ed25519` 将密钥加入代理

---

## 四、第二步：准备部署文件

### 4.1 确定要部署的内容

本案例中需要部署的文件在：
```
~/Desktop/AI工作台/
├── workbench.html        ← 主网页（内嵌数据，单文件即可运行）
├── ai-data-2026-09-22.json ← 原始数据（可选，用于备份）
└── AI日报-2026-09-22.html  ← 详细日报（可选）
```

### 4.2 创建本地仓库目录

终端运行：
```bash
# 创建部署目录
mkdir -p ~/pi/workbench-deploy
cd ~/pi/workbench-deploy

# 复制文件
cp ~/Desktop/AI工作台/workbench.html .
cp ~/Desktop/AI工作台/ai-data-*.json .
cp ~/Desktop/AI工作台/AI日报-*.html . 2>/dev/null || true
```

### 4.3 创建 GitHub Pages 必需文件

#### 4.3.1 创建 index.html（入口文件）

GitHub Pages 需要一个 `index.html` 作为网站入口：
```bash
cat > index.html << 'EOF'
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI 工作台</title>
    <script>
        window.location.href = 'workbench.html';
    </script>
</head>
<body>
    <p>正在跳转到 AI 工作台...</p>
</body>
</html>
EOF
```

#### 4.3.2 创建 .nojekyll（禁用 Jekyll）

告诉 GitHub 不要尝试构建 Jekyll：
```bash
echo "" > .nojekyll
```

#### 4.3.3 初始化 Git 仓库

```bash
cd ~/pi/workbench-deploy
git init
git config user.email "你的邮箱@example.com"
git config user.name "你的名字"
git add .
git commit -m "Initial commit: AI 工作台首页"
```

---

## 五、第三步：推送到 GitHub

### 5.1 添加远程仓库

终端运行（将 `<你的用户名>` 替换为你的 GitHub 用户名）：
```bash
git remote add origin git@github.com:<你的用户名>/ai-workbench.git
```

验证是否添加成功：
```bash
git remote -v
```
应输出：
```
origin  git@github.com:<你的用户名>/ai-workbench.git (fetch)
origin  git@github.com:<你的用户名>/ai-workbench.git (push)
```

### 5.2 推送 main 分支

```bash
git push -u origin main
```

首次推送可能需要输入 passphrase（如果生成密钥时设置了密码），否则直接推送成功。

成功后终端显示：
```
Enumerating objects: 5, done.
Counting objects: 100% (5/5), done.
Delta compression using up to 12 threads
Compressing objects: 100% (4/4), done.
Writing objects: 100% (5/5), 10.50 KiB | 5.25 MiB/s, done.
Total 5 (delta 0), reused 0 (delta 0), pack-reused 0
To github.com:<你的用户名>/ai-workbench.git
 * [new branch]      main -> main
branch 'main' set up to track 'origin/main'.
```

### 5.3 创建并推送 gh-pages 分支

GitHub Pages 需要在一个专门的分支上部署（通常是 `gh-pages` 或 `main`）：
```bash
# 创建 gh-pages 分支
git checkout -b gh-pages

# 确保有入口文件
echo "" > .nojekyll
cat > index.html << 'EOF'
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI 工作台</title>
    <script>
        window.location.href = 'workbench.html';
    </script>
</head>
<body>
    <p>正在跳转到 AI 工作台...</p>
</body>
</html>
EOF

# 提交并推送
git add .
git commit -m "deploy: gh-pages branch for GitHub Pages"
git push -u origin gh-pages
```

### 5.4 验证

打开浏览器访问：
```
https://github.com/<你的用户名>/ai-workbench
```
确认所有文件已上传，特别是 `gh-pages` 分支。

---

## 六、第四步：启用 GitHub Pages

### 6.1 访问 Pages 设置

1. 打开浏览器，访问你的仓库设置页面：
   ```
   https://github.com/<你的用户名>/ai-workbench/settings/pages
   ```

2. 如果页面加载慢或 Save 按钮是灰色的，按 **Cmd + Shift + R** 强制刷新

### 6.2 配置 Pages 设置

在 Pages 设置页面，按以下要求填写：

| 选项 | 值 | 说明 |
|------|-----|------|
| **Source** | **Deploy from a branch** | 从分支部署 |
| **Branch** | **gh-pages** | 选择 gh-pages 分支 |
| **Folder** | **/(root)** | 使用根目录 |

### 6.3 启用 Pages

1. 确认配置无误后，点击蓝色按钮 **Save**
2. 等待约 30 秒到 1 分钟
3. 页面顶部会显示：
   ```
   Your site is live at https://<你的用户名>.github.io/ai-workbench/
   ```

### 6.4 访问网站

点击链接或访问：
```
https://<你的用户名>.github.io/ai-workbench/
```
应该能看到 AI 工作台首页，自动跳转到 workbench.html。

---

## 七、日常维护与更新

### 7.1 自动更新流程

每天早晨 8:00，AI 工作台会自动完成以下操作：
1. 抓取最新 AI 资讯
2. 生成新的 workbench.html
3. 推送到 GitHub main 分支
4. 手动触发 gh-pages 分支更新

### 7.2 手动更新步骤

如需手动推送新内容：

```bash
# 1. 更新 main 分支
cd ~/pi/workbench-deploy
cp ~/Desktop/AI工作台/workbench.html .
cp ~/Desktop/AI工作台/ai-data-*.json .
git add .
git commit -m "更新: $(date +%Y-%m-%d)"
git push origin main

# 2. 切换到 gh-pages 分支并推送
git checkout gh-pages
git pull origin gh-pages
cp ~/Desktop/AI工作台/workbench.html .
git add .
git commit -m "更新: $(date +%Y-%m-%d)"
git push origin gh-pages
```

### 7.3 一键更新脚本

可以创建一个脚本简化更新流程：
```bash
cat > ~/pi/workbench-deploy/update.sh << 'EOF'
#!/bin/bash
set -e

WORKDIR="$HOME/Desktop/AI工作台"
DEPLOY_DIR="$HOME/pi/workbench-deploy"
TODAY=$(date +%Y-%m-%d)

echo "🚀 开始更新 AI 工作台..."

# 更新 main 分支
cd "$DEPLOY_DIR"
git checkout main
cp "$WORKDIR/workbench.html" .
cp "$WORKDIR/ai-data-${TODAY}.json" . 2>/dev/null || true
git add .
git commit -m "更新: $TODAY" || echo "💾 无变更"
git push

# 更新 gh-pages 分支
git checkout gh-pages
cp "$WORKDIR/workbench.html" .
git add .
git commit -m "更新: $TODAY" || echo "💾 无变更"
git push

echo "✅ 更新完成！访问: https://<你的用户名>.github.io/ai-workbench/"
EOF
chmod +x ~/pi/workbench-deploy/update.sh
```

---

## 八、常见问题排查

### Q1：手机端打开显示空白

**原因**：浏览器缓存了旧版页面
**解决**：
- iOS Safari：长按刷新按钮 → 清空网站数据
- Android Chrome：设置 → 隐私 → 清除浏览数据
- 或使用无痕模式访问

### Q2：Save 按钮是灰色的

**原因**：GitHub 页面加载慢或缓存问题
**解决**：
1. 按 **Cmd + Shift + R** 强制刷新
2. 等待 1-2 分钟后重试
3. 使用无痕浏览器模式
4. 确认 gh-pages 分支已推送成功

### Q3：网站打不开或显示 404

**原因**：GitHub Pages 需要时间部署
**解决**：
1. 等待 2-3 分钟
2. 检查 gh-pages 分支是否有 index.html 文件
3. 访问 GitHub Actions 查看构建日志

### Q4：GitHub 推送被拒绝

**原因**：SSH 权限问题或分支冲突
**解决**：
```bash
# 检查 SSH 连接
ssh -T git@github.com

# 重新添加密钥到代理
ssh-add ~/.ssh/id_ed25519

# 强制推送（谨慎使用，会覆盖远端）
git push --force
```

---

## 九、费用说明

| 项目 | 费用 | 说明 |
|------|------|------|
| GitHub 仓库 | 免费 | 无限公开仓库 |
| GitHub Pages | 免费 | 每月 100GB 流量 |
| 自定义域名 | 域名本身费用 | 如阿里云购买约 50 元/年 |

**总计：完全免费**

---

## 附录：快速命令参考

```bash
# ========== 生成 SSH Key ==========
ssh-keygen -t ed25519 -C "workbench" -f ~/.ssh/id_ed25519 -N ""

# ========== 查看公钥（复制到 GitHub） ==========
cat ~/.ssh/id_ed25519.pub

# ========== 验证 SSH 连接 ==========
ssh -T git@github.com

# ========== 推送更新到 main 分支 ==========
cd ~/pi/workbench-deploy
git checkout main
git pull
# 复制最新文件...
git add .
git commit -m "更新: $(date +%Y-%m-%d)"
git push

# ========== 推送更新到 gh-pages 分支 ==========
git checkout gh-pages
git pull
# 复制最新文件...
git add .
git commit -m "更新: $(date +%Y-%m-%d)"
git push

# ========== 查看部署状态 ==========
curl -s https://api.github.com/repos/<用户名>/ai-workbench/pages
```

---

## 十、文档版本历史

| 日期 | 版本 | 变更说明 |
|------|------|----------|
| 2026-09-22 | v1.0 | 初始版本，使用 Cloudflare Pages 方案 |
| 2026-09-22 | v1.1 | 改用 GitHub Pages 方案，修复 Cloudflare 访问问题 |

---

*本文档基于实际部署经验编写，如有问题可随时调整。*
