# AI 工作台 — Cloudflare Pages 免费部署操作手册

> 适用系统：macOS  
> 目标：将本地 AI 工作台发布为可手机端访问的免费网站

---

## 前置条件

- 已注册 GitHub 账号（用户名：`yancx8702`）
- 已注册 Cloudflare 账号
- 本地已安装 Git、Python3

---

## 第一步：GitHub 上创建仓库

1. 打开浏览器，访问：
   ```
   https://github.com/new
   ```

2. 填写信息：
   - **Repository name**：`ai-workbench`
   - **Owner**：选择你的账号 `yancx8702`
   - **Visibility**：Public（公开）
   - ❌ **不勾选** "Add a README file"
   - 点击绿色按钮 **Create repository**

3. 创建完成后，页面会显示仓库空地址，记住右上角的 SSH 地址：
   ```
   git@github.com:yancx8702/ai-workbench.git
   ```

---

## 第二步：生成 SSH 密钥并添加到 GitHub

### 2.1 查看本地是否已有 SSH 密钥

打开终端（Terminal.app），运行：
```bash
ls ~/.ssh/id_*.pub
```
- 如果输出了文件路径（如 `/Users/xxx/.ssh/id_ed25519.pub`），说明已有密钥，跳到 **2.3**
- 如果提示 `No such file or directory`，继续下一步

### 2.2 生成新的 SSH 密钥

终端运行：
```bash
ssh-keygen -t ed25519 -C "ai-workbench" -f ~/.ssh/id_ed25519 -N ""
```
- `-t ed25519`：使用安全算法
- `-C "ai-workbench"`：密钥备注
- `-f ~/.ssh/id_ed25519`：保存到默认位置
- `-N ""`：不设密码（免交互）

### 2.3 复制公钥内容

终端运行：
```bash
cat ~/.ssh/id_ed25519.pub
```
终端会输出一行类似：
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIB... ai-workbench
```
**全选复制这一整行**

### 2.4 添加到 GitHub

1. 打开：
   ```
   https://github.com/settings/keys
   ```
2. 点击绿色按钮 **New SSH key**
3. **Title** 填写：`ai-workbench`
4. **Key** 粘贴刚才复制的内容
5. 点击 **Add SSH key**

### 2.5 验证连接成功

终端运行：
```bash
ssh -T git@github.com
```
出现以下提示说明成功：
```
Hi yancx8702! You've successfully authenticated, but GitHub does not provide shell access.
```

---

## 第三步：推送代码到 GitHub

### 3.1 确认部署目录已有文件

终端运行：
```bash
ls ~/pi/workbench-deploy/
```
应看到 4 个文件：
```
workbench.html
ai-data-2026-09-22.json
AI日报-2026-09-22.html
README.md
```

### 3.2 执行推送

终端运行：
```bash
cd ~/pi/workbench-deploy
git remote set-url origin git@github.com:yancx8702/ai-workbench.git
git push -u origin main
```
首次推送成功后，终端显示：
```
Enumerating objects: ...
Counting objects: ...
Writing objects: ...
To github.com:yancx8702/ai-workbench.git
 * [new branch]      main -> main
branch 'main' set up to track 'origin/main'.
```

### 3.3 验证推送成功

打开浏览器访问：
```
https://github.com/yancx8702/ai-workbench
```
应该能看到所有文件都在仓库里。

---

## 第四步：Cloudflare Pages 部署

### 4.1 登录 Cloudflare

1. 打开：
   ```
   https://dash.cloudflare.com/pages
   ```
2. 用你的 Cloudflare 账号登录（没有则注册：https://dash.cloudflare.com/sign-up）

### 4.2 创建 Pages 项目

1. 点击蓝色按钮 **Create project**
2. 在"Connect to Git"区域，点击 **Continue to project**（或直接选择 GitHub）
3. 选择 **GitHub** 作为 Git 提供商
4. 点击 **Connect** 授权 Cloudflare 访问 GitHub
5. 在仓库列表中找到并选中 **ai-workbench**
6. 点击 **Begin setup**

### 4.3 配置构建参数

在配置页面填写以下内容：

| 选项 | 填写值 |
|------|--------|
| **Production branch** | `main` |
| **Framework preset** | 选择 **None** |
| **Build command** | *(留空，不要填)* |
| **Build output directory** | `.` （一个点，表示当前目录） |
| **Environment variables** | *(留空)* |

> ⚠️ 重要：`Build output directory` 必须填 `.` ，不是空也不是 `./`

### 4.4 部署

1. 确认配置无误后，点击底部蓝色按钮 **Save and Deploy**
2. Cloudflare 会自动从 GitHub 拉取代码并部署
3. 等待约 1-2 分钟，页面上会显示部署状态（Building → Deployed）
4. 部署完成后，点击 **Visit site** 或复制提供的链接

### 4.5 确认访问

你会得到一个类似这样的网址：
```
https://ai-workbench-xxx.pages.dev
```

- 在电脑浏览器打开 → 正常显示
- 在手机浏览器打开同一链接 → 同样正常显示

---

## 第五步（可选）：自定义域名

如果你有自己的域名：

1. 在 Cloudflare Pages 项目页面，点击 **Custom domains**
2. 点击 **Set up a custom domain**
3. 输入你的域名（如 `news.yourdomain.com`）
4. 按提示在域名 DNS 中添加 CNAME 记录指向 `cname.pages.dev`

---

## 日常更新流程

每天早晨 8:00，AI 工作台自动完成以下操作：
1. 抓取 4 个数据源的 AI 资讯
2. 用 Agnes 智能分析并分级
3. 生成新的 workbench.html
4. 自动推送到 GitHub
5. Cloudflare Pages 自动重新部署

如需手动触发，终端运行：
```bash
cd ~/AI-News-Agent && python3 main.py --quiet
cd ~/pi/workbench-deploy && git add . && git commit -m "手动更新" && git push
```

---

## 常见问题

**Q：手机端打开显示空白？**  
A：检查是否是旧版缓存，强制刷新（iOS Safari 长按刷新按钮 → 清空缓存与数据）

**Q：Cloudflare 部署失败？**  
A：回到 Pages 项目 → Deployments → 查看报错日志，通常是构建目录配置错误

**Q：GitHub 推送被拒绝？**  
A：确认 SSH key 已添加，且仓库名拼写正确（注意大小写）
