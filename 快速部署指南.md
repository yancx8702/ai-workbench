# AI 工作台 - 快速部署指南

## 方案对比

| 方案 | 优点 | 缺点 | 最终选择 |
|------|------|------|----------|
| **GitHub Pages** | 免费、无需额外账号、与代码同源 | 自定义域名配置稍复杂 | ✅ **已采用** |
| Cloudflare Pages | 功能更强、CDN 更快 | 需要注册账号、国内访问可能受限 | ❌ 尝试过但因网络问题放弃 |

---

## 快速方案：手动启用 GitHub Pages（30秒）

### 第 1 步：打开仓库设置
访问：
```
https://github.com/yancx8702/ai-workbench/settings/pages
```

### 第 2 步：启用 Pages
1. **Source** 选择 `Deploy from a branch`
2. **Branch** 选择 `gh-pages`
3. **Folder** 选择 `/(root)`
4. 点击 **Save**

> ⚠️ 如果 Save 按钮是灰色的，按 **Cmd + Shift + R** 强制刷新页面再试

### 第 3 步：等待部署
约 30 秒到 1 分钟后，页面顶部会显示：
```
Your site is live at https://yancx8702.github.io/ai-workbench/
```

### 第 4 步：验证
访问以下链接，应该能看到 55 条 AI 资讯：
```
https://yancx8702.github.io/ai-workbench/workbench.html
```

---

## 自动方案：使用 GitHub Token（需要 2 分钟配置）

如果你希望以后每次推送都能自动启用 Pages，可以：

1. 生成 Token：https://github.com/settings/tokens/new
   - Name: `ai-workbench-pages`
   - Expiration: 1 year
   - Scopes: 勾选 `repo`
   
2. 运行脚本：
```bash
export GITHUB_TOKEN=ghp_xxxxxxxxxxxx
bash ~/pi/workbench-deploy/启用-GitHub-Pages.sh
```

---

## 日常更新流程

### 自动更新（每天 8:00）
AI 工作台会自动：
1. 抓取最新资讯
2. 推送到 GitHub main 分支

### 手动更新
如需立即更新网站内容：
```bash
# 更新 main 分支
cd ~/pi/workbench-deploy
git checkout main
# 复制最新文件...
git add .
git commit -m "更新: $(date +%Y-%m-%d)"
git push

# 更新 gh-pages 分支
git checkout gh-pages
# 复制最新文件...
git add .
git commit -m "更新: $(date +%Y-%m-%d)"
git push
```

---

## 常见问题

**Q: Save 按钮是灰色的怎么办？**
A: 按 Cmd + Shift + R 强制刷新，或等待 1-2 分钟后再试。

**Q: 网站打不开？**
A: 等待 2-3 分钟，GitHub Pages 首次部署需要时间。

**Q: 手机打不开？**
A: 确保链接是 `https://` 开头的，手机浏览器直接输入即可。

**Q: 想绑定自己的域名？**
A: 在 GitHub Pages 设置中添加 CNAME 文件，指向你的域名。

---

## 网站访问地址

部署完成后，网站地址为：
```
https://yancx8702.github.io/ai-workbench/
```

手机和电脑都可以访问，无需安装任何应用。
